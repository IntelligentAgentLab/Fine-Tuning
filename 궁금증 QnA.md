# 이론 노트: 파인튜닝을 공부하며 생긴 질문들

> 실습(맥 스튜디오)을 진행할 수 없는 동안 이론 질문과 답을 정리해두는 문서.
> 질문 → 답 구조로 쌓아간다.

---

## Q1. 큰 모델로 Q&A 데이터를 생성한다는데, GPT-3 이전에는 어떻게 했나?

**나의 질문**: 실무 데이터 파이프라인이 "문서 → 큰 모델로 Q&A 생성 → 검수 → 학습"이라고 하는데, 모델이 발전하지 않았던 시절에는 이런 방식을 쓸 수 없었을 것이다. 그럼 어떻게 발전시켜온 것인가?

**답**: 질문 자체가 정확히 급소를 짚은 것. "강한 모델로 데이터를 만든다면 최초의 강한 모델은 어떻게 만들었나"는 **부트스트래핑(bootstrapping) 문제**이고, 실제 역사가 그 답이다.

| 시기 | 데이터를 만든 방법 | 대표 사례 |
|---|---|---|
| 1기 (~2018) | **전부 사람이 수작업** - 전문가 태깅, 크라우드소싱으로 Q&A를 손으로 작성 | SQuAD(2016): 위키 문서를 읽고 10만 쌍 작성. KorQuAD도 동일 방식 |
| 2기 (2018~2020) | **사전학습의 돌파구**: "다음 토큰 맞히기"는 텍스트 자체가 정답이라 라벨링 불필요(자기지도학습) → 데이터 병목 소멸 | BERT, GPT-1/2 |
| 3기 (2020~2022) | 챗 모델의 씨앗 데이터도 **고용된 사람이 직접 작성** (이상적 답변 시연 + 답변 순위 매기기) | InstructGPT의 SFT/RLHF 데이터 |
| 4기 (2022~) | **모델이 모델의 데이터를 생성**: 소수의 인간 시드 → 강한 모델이 대량 생성 | Self-Instruct, Alpaca(2023): 시드 175개 → GPT-3가 5.2만 건 생성 |

-> 부트스트래핑의 사슬: 인터넷의 인간 텍스트(라벨 불필요) → 사전학습으로 첫 강한 모델 → 소수의 인간 시연으로 어시스턴트화 → 그 모델로 합성 데이터 대량 생산 → 다음 세대. **각 세대가 다음 세대의 데이터 공장이 되는 구조.**

-> 우리가 쓰려는 "문서 → 큰 모델로 Q&A 생성" 파이프라인은 Alpaca 계보. 2019년이었다면 사람을 고용해 규정집을 읽고 Q&A를 쓰게 했을 것이다.

**추가로 알아둘 것**: 지금도 최고급 데이터는 사람이 만든다. 프런티어 모델 회사들은 박사급 전문가를 고용해 수학 풀이·코드 리뷰 데이터를 작성시킨다. 합성 데이터는 "규모"를, 인간 데이터는 "천장"을 담당한다.

---

## Q2. 모델이 자기 학습 데이터를 직접 생성하면 문제가 생기지 않나?

**나의 질문**: 모델이 진리와 다른 답변을 생성하고, 그 데이터로 또 학습하고 또 생성하는 과정이 반복되면, 정확도가 안 좋은 쪽으로 튕겨 나갈 수도 있지 않을까? 과적합이나 발산 같은 문제가 있을 것 같다.

**답**: 정확한 직관이고, 실제로 이름이 붙은 연구 주제다 - **모델 붕괴(model collapse)**. 실패 모드는 두 갈래.

1. **오류 증폭** (내가 말한 시나리오): 틀린 답 생성 → 학습 데이터가 됨 → 다음 모델이 "사실"로 학습 → 또 생성. 세대를 거칠수록 오류가 복리로 쌓임. 복사본의 복사본을 만들수록 화질이 망가지는 구조.
2. **분포 수축** (덜 직관적이지만 더 근본적): 오류가 없어도 붕괴는 일어난다. 모델은 확률 높은 것 위주로 생성하므로 생성 데이터에는 원래 분포의 "꼬리"(드문 표현, 소수 사례)가 빠져 있음 → 세대마다 꼬리가 얇아지고 출력이 평균적·획일적으로 수렴. 내가 "발산"이라 표현한 것은 수학적으로는 **수축**에 가깝고, 진리에서 멀어진다는 점에서 체감상 발산처럼 보이는 것. (2024년 Nature 게재 연구로 유명해진 주제)

**그럼 왜 실무 합성 데이터 파이프라인은 안 무너지나?** → 순수한 자기 루프가 아니기 때문. 붕괴를 막는 장치가 몇 겹 있다.

| 장치 | 내용 |
|---|---|
| 외부 진리 앵커 | "문서 → Q&A 생성"은 모델이 지어내는 게 아니라 **실제 문서에 근거해 변환**만 하는 것. 진리의 원천은 여전히 인간이 쓴 문서. 실제 데이터를 계속 섞으면 붕괴가 크게 완화됨 |
| 외부 검증기 | 수학·코드처럼 **정답을 모델 바깥에서 기계적으로 채점 가능한** 영역에서 합성 데이터가 특히 잘 작동. 틀린 생성물을 버리면 오류가 다음 세대로 전파될 통로가 막힘. 추론 모델의 RL이 성립하는 이유 (보상이 모델 자신이 아니라 외부 검증기에서 나옴) |
| 검수·필터링 | "생성 → 검수 → 학습"에서 검수가 괜히 있는 게 아님. 품질 점수화, 중복 제거, 사람 샘플링 검토 |
| 방향성 | 실무 증류는 보통 "강한 선생 → 약한 학생"의 내리막 방향. 자기가 만든 데이터를 자기가 먹는 순수 루프와는 위험 구조가 다름 |

-> 정리: 아무 장치 없는 자기 소비 루프에서는 내 우려가 실제로 일어난다. 실무는 그걸 알기 때문에 **진리 앵커 + 외부 검증 + 인간 검수**로 루프를 끊어놓은 것이다.

**추가로 알아둘 것**: 이건 여전히 열린 문제다. 인터넷 자체에 AI 생성 텍스트가 쌓이면서 다음 세대 사전학습 크롤링 데이터가 오염되는 문제는 업계 전체의 고민거리.

---

*(이후 질문이 생기면 Q3, Q4... 로 이어서 추가)*
