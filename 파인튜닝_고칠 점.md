# 파인튜닝 기초 정리

## 1. 지난 시도, 무엇이 문제였나

**했던 것**: Question, Answer로 구성된 두 개의 컬럼으로 구성된 데이터를 Qwen2.5-8b Instruct 모델에 그대로 학습을 시도 → 데이터 셋에 있는 질문을 했음에도 불구하고 답변이 잘못 나오거나, 중국어가 나오는 등의 문제가 있었다 → epoch를 수정하거나, 학습 정도 수치를 수정했지만 해결방법을 알 수가 없었다.

**결과**: 중국어가 섞여 나오고, 학습시킨 답변은 안 나옴.

**근본 오해**: "파인튜닝하면 RAG처럼 모델이 내 데이터를 찾아서 답해줄 것"이라는 기대.
하지만 파인튜닝은 지식 검색기를 만드는 게 아니라, 모델의 **말하는 경향(확률)을 내 데이터 쪽으로 기울이는 것**이다. 지식 암기는 부수 효과일 뿐이고, 무리하게 암기시키려 하면 부작용이 먼저 온다.

**당시의 나의 생각**: 파인튜닝과 RAG가 무엇인지 전부 이론상으로는 알고 있었으나, 각 방법론에 대한 활용 방법이 잘못됐다.
-> 즉, 앞서 말했듯이 파인튜닝은 RAG를 통해 추출한 데이터를 잘 가져오게 하는, 혹은 가져온 데이터를 사용자의 질문에 맞는 형태로 답변을 할 확률을 높이는 과정인 것이다.

문제점 3가지:

| 문제 | 결과 |
|---|---|
| 챗 템플릿을 찾아 학습을 했지만, 템플릿을 이용한 학습 방법에 문제가 있었던 것 같다. '''system, user, assistant 형식에 맞게 데이터를 템플릿에 맞게 넣어서 제대로 학습해볼 것''' | 학습한 형식 ≠ 질문받는 형식 → 학습 내용이 안 나옴 |
| 검증셋 없이 에포크만 조정 | 과적합(암기) 시작 시점을 볼 수 없음 |
| 잘못된 형식으로 반복 학습 | 대화 훈련 층이 깎임 → 사전학습의 중국어가 새어 나옴 (파국적 망각) |

## 2. 실제 회사들은 어떻게 설계하나: RAG vs 파인튜닝

둘은 경쟁 관계가 아니라 **역할이 다른 부품**이다.

| | RAG | 파인튜닝 |
|---|---|---|
| 방식 | 답변 시점에 사내 문서를 **검색**해서 프롬프트에 넣어줌 | 모델 자체를 추가 **학습** |
| 담당 | **사실, 최신 정보** ("연차 규정 몇 조에 뭐라고 써 있나") | **말투, 형식, 행동** ("우리 회사 톤으로, 이런 구조로 답해라") '''파인튜닝을 처음 접하는 사람의 입장에서 [말투, 형식, 행동]이라고 말을 하면 정확히 와닿지 않을 때가 많다. 상황에 맞는 판단과 답변을 할 수 있는 논리력을 기르는 과정이라고 이해하면 더 좋을 것 같다.'''|
| 정보 갱신 | 문서만 바꾸면 즉시 반영 | 재학습 필요 |
| 환각 위험 | 근거 문서가 있어 낮음 | 애매하게 외운 걸 지어낼 수 있음 |

**실무 표준 설계**: 사내 지식·규정·최신 정보는 RAG로 공급하고, 파인튜닝은 답변 스타일·용어·거절 규칙 같은 "행동"을 다듬는 데 쓴다. 많은 회사가 RAG만으로 시작하고, 파인튜닝은 필요할 때 더한다. 추가적으로 많은 회사들이 파인튜닝을 하는 방향보다도 RAG를 더 잘 활용할 수 있는 에이전트를 덧붙이는 것을 목표로 한다는 것을 들은 기억이 있다.

→ 즉 "조직 지식에 답하는 AI"의 최종 형태는 RAG가 중심이 될 가능성이 높다. 다만 파인튜닝을 끝까지 해보는 건 그 자체로 배울 가치가 있다 (지금 목표).

## 3. 파인튜닝 데이터의 기본: system / user / assistant

채팅 모델(Instruct 모델)은 아래 3가지 역할로 된 **대화 형식**으로 훈련된 모델이다.

- **system**: AI의 역할·규칙 지정 ("너는 ○○사 사내 어시스턴트다. 한국어로 답해라")
- **user**: 사용자 질문
- **assistant**: 모델의 답변 ← 학습 때 모델이 실제로 배우는 부분

그래서 학습 데이터 1건은 raw Q/A가 아니라 이렇게 만들어야 한다:

```json
{"messages": [
  {"role": "system", "content": "당신은 사내 업무 어시스턴트입니다. 한국어로 답하세요."},
  {"role": "user", "content": "연차는 어떻게 신청하나요?"},
  {"role": "assistant", "content": "HR 포털의 근태관리 메뉴에서 신청합니다. ..."}
]}
```

학습 시 프레임워크가 이걸 모델 고유의 **챗 템플릿**으로 렌더링한다 (Qwen의 경우 - 모델마다 템플릿이 다르니 참고할 것):

```
<|im_start|>system
당신은 사내 업무 어시스턴트입니다. 한국어로 답하세요.<|im_end|>
<|im_start|>user
연차는 어떻게 신청하나요?<|im_end|>
<|im_start|>assistant
HR 포털의 근태관리 메뉴에서 신청합니다. ...<|im_end|>
```

## 4. 그 외 기본 수칙

- **데이터 3분할**: train(학습) / valid(학습 중 과적합 감시) / test(최종 평가 전용)
- **val loss가 계기판**: train loss는 계속 내려가도, val loss가 오르기 시작하면 과적합 → 그 이전 체크포인트가 최선
- **에포크는 감으로 조정하지 않는다**: 반복 = 이해가 아니라 암기
- **학습 전 데이터 검증**: 중국어 오염, 빈 값, 중복, 너무 짧은 답변은 미리 걸러낸다 (오염된 데이터 = 오염된 모델)
- **LoRA 사용**: 원본 파라미터는 얼리고 작은 어댑터만 학습 → 망각 위험↓, 메모리↓. 맥에서는 MLX + LoRA가 표준
- **비교 기준선**: 파인튜닝 전 원본 모델에 같은 질문을 먼저 던져 기록해둔다. 이게 없으면 뭐가 좋아졌는지 알 수 없다
